services:
  - type: web
    name: double-agent-webhook
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn scripts.webhook_service:app
    envVars:
      - key: PORT
        value: 5000
      - key: WEBHOOK_SERVICE_SECRET
        scope: runtime
        sync: false
